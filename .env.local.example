# Copy to .env.local if you need to override the API URL.
# Local: leave unset to use /api (Next.js proxies to http://localhost:8000).
# Or set NEXT_PUBLIC_API_URL=http://localhost:8000 to call the API directly.
# Production: set to your deployed RAG API URL.
# NEXT_PUBLIC_API_URL=http://localhost:8000
